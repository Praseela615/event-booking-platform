const rows=['A','B','C','D','E','F'];let selected=[];const booked=['A2','B4','D1','E6'];function buildSeats(){const map=document.getElementById('seatMap');if(!map)return;map.innerHTML='';rows.forEach(r=>{for(let n=1;n<=6;n++){const id=r+n,b=document.createElement('button');b.className='seat'+(booked.includes(id)?' booked':'');b.textContent=id;b.disabled=booked.includes(id);b.onclick=()=>{b.classList.toggle('selected');selected.includes(id)?selected=selected.filter(x=>x!==id):selected.push(id);updateSeats()};map.appendChild(b)}})}function updateSeats(){document.getElementById('selectedSeats').textContent=selected.length?selected.join(', '):'None';document.getElementById('seatTotal').textContent='₹'+(selected.length*999).toLocaleString('en-IN')}function continueSeats(){if(!selected.length)return alert('Please select at least one seat.');localStorage.setItem('eventnova-seats',JSON.stringify(selected));location.href='booking.html'}buildSeats();